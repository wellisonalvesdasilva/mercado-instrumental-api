server.port=${SERVER_PORT:8080}

spring.application.name=mercado-instrumental
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:mercado-instrumental-dsv}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

server.servlet.context-path=/v1/api
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=none

jwt.secret=${JWT_SECRET:1234567890123456789012345678901234567890123456789012345678901234}
jwt.expiration=${JWT_EXPIRATION:3600000}

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.url=/v3/api-docs

upload.imagem.diretorio=${UPLOAD_IMAGEM_DIRETORIO:/caminho/padrao/imagens}
clients.rws-ibge.url=${CLIENTS_RWS_IBGE_URL:https://servicodados.ibge.gov.br/api/v1}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:mercadoinstrumental@gmail.com}
spring.mail.password=${MAIL_PASSWORD:grua dhlu zrox gcdv}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.protocol=${MAIL_PROTOCOL:smtp}

empresa.nome=${EMPRESA_NOME:Mercado Instrumental}
aplicacao-web=${APLICACAO_WEB:http://www.mercadoinstrumental.com.br}
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}
logging.level.root=DEBUG